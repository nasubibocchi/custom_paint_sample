import 'package:flutter/cupertino.dart';

const diagonalButtonSize = Size(150, 50);
const normalButtonSize = Size(150, 50);

const restrictedButtonSize = Size(100, 100);

const doubleLineButtonSize = Size(150, 50);